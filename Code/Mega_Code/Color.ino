
// Sensor read values
int redReading;
int greenReading;
int blueReading;
int clearReading;

// Sensor ranges for each color
int redLow, redHigh;
int blueLow, blueHigh;
int greenLow, greenHigh;
int clearLow, clearHigh;

/* Setps up the color sensor with its pre-calibrated values. */
void colorSetup() {

}

/*  Prompts the color sensor for a reading and checks if that reading matches the
 *  calibrated definition of red.
 *  Returns true if the block is red, false otherwise. */
bool isRed() {

}

/*  Prompts the color sensor for a reading and checks if that reading matches the
 *  calibrated definition of blue.
 *  Returns true if the block is blue, false otherwise. */
bool isBlue() {

}

/*  Prompts the color sensor for a reading and checks if that reading matches the
 *  calibrated definition of green.
 *  Returns true if the block is green, false otherwise. */
bool isGreen() {

}

/*  Gets a reading from the color sensor and assigns the read values to the color-value variables. */
void readColor() {

}